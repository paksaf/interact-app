# E2E encryption — Phase 1.5 sprint (2026-09-02)

**Build started:** `0.5.25+6069`  
**Status:** Identity + envelope + pre-key API client scaffolded. **User-facing encrypt still OFF.**

## Goal

1:1 Signal-protocol ciphertext for cloud chat path, with honest offline bearer behavior documented separately.

## Client (interact-app)

| Item | File | Status |
|------|------|--------|
| libsignal dep | `pubspec.yaml` → `libsignal_protocol_dart` | ✅ added |
| Wire envelope | `lib/services/e2e/e2e_envelope.dart` | ✅ `e2e:v1:` prefix |
| Identity install | `lib/services/e2e/e2e_identity_manager.dart` | ✅ secure storage |
| Pre-key API client | `lib/services/e2e/e2e_prekey_api.dart` | ✅ fail-soft |
| Crypto facade | `lib/services/e2e_crypto_service.dart` | ✅ gated `INTERACT_E2E` |
| Session encrypt/decrypt | same | ☐ next step |
| UI lock icon + safety number | chat thread | ☐ |
| Offline frame ciphertext | `OfflineFrame.body` | ☐ |

## Compile-time gate

```bash
flutter run --dart-define=INTERACT_E2E=true
```

Default builds: **`shouldEncryptOutbound = false`** — no behavior change for lab devices.

## Backend (qurbanisahulat — coordinate Sahulat freeze)

| Endpoint | Method | Purpose |
|----------|--------|---------|
| `/api/v1/talk/e2e/prekeys` | POST | Upload identity + OTPKs + signed pre-key |
| `/api/v1/talk/e2e/prekeys/:userId` | GET | Fetch peer bundle for session build |

Server stores public material only; never private keys.

## Sprint order (remaining)

1. **Sahulat routes** — pre-key CRUD + JWT auth  
2. **SessionBuilder** — fetch peer bundle → encrypt outbound  
3. **SessionCipher decrypt** — inbound in `message_repository`  
4. **Upload on install** — after `E2eIdentityManager.install()`  
5. **UI** — Me → E2E status, thread lock icon  
6. **Field** — E2E-1: two devices, cloud path, ciphertext only on wire body  

## Honest limits (6069)

- Groups: not E2E (banner already planned in roadmap)  
- Offline LAN/BLE: plaintext until offline ciphertext spec  
- Server metadata (who talked to whom): still visible — Sealed Sender is later  

## Tests

- `test/e2e_envelope_test.dart`  
- `test/chat_phase2_test.dart` — stub expectations unchanged for default builds  
