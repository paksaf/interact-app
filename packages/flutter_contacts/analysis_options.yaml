# Vendored copy — the upstream include (package:pedantic, discontinued 2021)
# is not in our dependency graph and produced an include_file_not_found
# warning on every `flutter analyze` (2026-08-28). No include: default rules.
